# Assignment 8 — Express + MongoDB

Quick start:

1. Install dependencies:

```bash
npm install
```

2. Start MongoDB locally or provide `MONGODB_URI` env var.

3. Run the server:

```bash
npm start
```

Server listens on port 3000 by default. Use the endpoints specified in the assignment.
